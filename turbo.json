{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/master",
  "pipeline": {
    "//#build:all": {
      "inputs": ["**/*.ts", "**/*.tsx", "**/tsconfig*", "tsconfig*"]
    },
    "build": {
      "inputs": ["**/*.ts", "**/*.tsx", "**/webpack.config.*"],
      "dependsOn": ["//#build:all", "^build"]
    },
    "//#dev:all": {
      "outputs": ["**/dist/**"],
      "inputs": ["**/*.ts", "**/*.tsx", "**/tsconfig*", "tsconfig*"],
      "dependsOn": []
    },
    "dev": {
      "outputs": ["**/dist/**"],
      "inputs": ["**/*.ts", "**/*.tsx", "**/webpack.config.*"],
      "dependsOn": ["build", "^dev"]
    },
    "//#test:all": {
      "inputs": ["**/*.test.ts", "**/*.test.tsx", "**/tsconfig*", "tsconfig*", ".mocharc.json"],
      "dependsOn": ["build"]
    },
    "test": {
      "inputs": ["**/*.test.ts", "**/*.test.tsx", "**/tsconfig*", "tsconfig*", ".mocharc.json"],
      "dependsOn": ["//#test:all", "^test"],
      "outputs": []
    },
    "//#lint:all": {
      "inputs": ["**/*.ts", "**/*.tsx", ".eslintrc"],
      "dependsOn": ["build"],
      "outputs": []
    },
    "lint": {
      "inputs": ["**/*.ts", "**/*.tsx"],
      "dependsOn": ["//#lint:all", "^lint"],
      "outputs": []
    },
    "//#clean": {
      "inputs": ["**/*.ts", "**/*.tsx"],
      "outputs": ["**/dist/**"]
    }
  }
}
